import fs from 'fs'; import path from 'path'; import { fileURLToPath } from 'url'; import { mockStudents } from './src/data/mockData.js'; const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); const dbPath = path.join(__dirname, 'server', 'database.json'); const initialData = { students: mockStudents }; fs.writeFileSync(dbPath, JSON.stringify(initialData, null, 2), 'utf-8'); console.log('Database initialized');
